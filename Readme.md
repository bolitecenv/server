// ソケット通信を使用したHTTPサーバー
//linux 向けに作成している
//windowsでは動作しないしないためWinSockを導入し、書き換える必要がある


*使用方法
*コマンドプロンプトにて以下のように実行
*ただし、ファイル保存先のディレクトリに移動して実行してください。

[host@localhost]$ gcc server.c http.c -o server
[host@localhost]$ ./server


Content type->
text/html
img/bmp
img/png


に対応している．

